spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:mysql://localhost:3306/testbankapp?createDatabaseIfNotExist=true
    username: root
    password: ZAH76kom20
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.xml
    drop-first: true